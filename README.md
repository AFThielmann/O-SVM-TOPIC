# O-SVM-TOPIC

## Script for unsupervised imbalanced document classification. 

Integration of out-of-domain labelled training data. One-class SVM is trained on this labelled training data with user-specified constriaints on the hpyer parameters. Target data is subsequently classified and classified data is further analyzed with LDA Topic modelling.
